# MDEditor 技术规格

## 1. 项目概述

轻量级外部编辑器，用于 Claude Code 的 `Ctrl+G` 功能。

### 技术栈
- **框架**: Tauri v2
- **前端**: 原生 HTML/CSS/JS
- **平台**: macOS

---

## 2. 核心流程

```
Claude Code                         MDEditor
    |                                   |
    |--- spawn "mdeditor <file>" ------>|
    |                                   | 1. 解析命令行参数获取文件路径
    |                                   | 2. 读取文件内容
    |                                   | 3. 打开窗口，显示编辑器
    |       (阻塞等待进程退出)            |
    |                                   | 4. 用户编辑...
    |                                   |
    |                                   | 5. 用户触发退出（关闭窗口/快捷键）
    |                                   | 6. 弹窗确认：保存/不保存/取消
    |                                   | 7. 保存文件（如选择保存）
    |<------ exit(0) 或 exit(1) --------|
    |
    | 读取文件内容，回填输入框
```

---

## 3. 命令行接口

```bash
mdeditor <file_path>
```

### 退出码
| 退出码 | 含义 |
|--------|------|
| 0 | 确认（保存并退出） |
| 1 | 取消（不保存退出） |
| 2+ | 错误 |

---

## 4. 窗口规格

- **尺寸**: 屏幕宽高 × 80%
- **位置**: 屏幕居中
- **可调整大小**: 是
- **标题**: 文件名
- **最小尺寸**: 400 × 300

---

## 5. UI 设计

```
┌─────────────────────────────────────────────┐
│  [标题栏: 文件名]                    [─][□][×] │
├─────────────────────────────────────────────┤
│                                             │
│  ┌─────────────────────────────────────┐   │
│  │                                     │   │
│  │         <textarea>                  │   │
│  │         纯文本编辑区域                │   │
│  │         等宽字体                     │   │
│  │                                     │   │
│  └─────────────────────────────────────┘   │
│                                             │
│  ┌─────────────────────────────────────┐   │
│  │ [取消]              [保存并返回 ⌘↵]  │   │
│  └─────────────────────────────────────┘   │
└─────────────────────────────────────────────┘
```

### 样式
- **字体**: 系统等宽字体（`SF Mono`, `Menlo`, `monospace`）
- **字号**: 14px
- **行高**: 1.5
- **背景色**: 系统默认

---

## 6. 交互行为

### 快捷键
| 快捷键 | 行为 |
|--------|------|
| `⌘ + Enter` | 保存并退出（exit 0） |
| `⌘ + W` | 触发关闭窗口流程 |

### 关闭窗口流程（有未保存更改时）

```
用户点击关闭/⌘W
       │
       ▼
┌──────────────────────────────┐
│  文件已修改，是否保存？         │
│                              │
│  [不保存]  [取消]  [保存]      │
└──────────────────────────────┘
       │
       ├── 保存 ──→ 写入文件 → exit(0)
       │
       ├── 不保存 ──→ exit(1)
       │
       └── 取消 ──→ 返回编辑器
```

### 无更改时关闭
- 直接 exit(0)

---

## 7. 文件处理

### 读取
- 从命令行参数获取文件路径
- UTF-8 编码读取

### 保存（原子写入）
1. 写入 `<file>.tmp`
2. `fsync` 确保落盘
3. `rename` 覆盖原文件

---

## 8. 状态管理

| 状态 | 说明 |
|------|------|
| `filePath` | 当前编辑的文件路径 |
| `originalContent` | 文件原始内容 |
| `currentContent` | 当前编辑器内容 |
| `isDirty` | `currentContent !== originalContent` |

---

## 9. Tauri 命令

| 命令 | 说明 |
|------|------|
| `get_file_path` | 获取命令行传入的文件路径 |
| `read_file` | 读取文件内容 |
| `save_file` | 原子保存文件 |
| `exit_app` | 以指定退出码退出 |

---

## 10. 错误处理

| 场景 | 处理方式 |
|------|----------|
| 无命令行参数 | 显示提示对话框，确定后 exit(2) |
| 文件不存在 | 显示错误对话框，确定后 exit(2) |
| 文件无读取权限 | 显示错误对话框，确定后 exit(2) |
| 保存失败 | 显示错误对话框，保持编辑器打开 |

---

## 11. 项目结构

```
mdeditor/
├── src-tauri/
│   ├── Cargo.toml
│   ├── tauri.conf.json
│   └── src/
│       ├── main.rs
│       ├── commands.rs
│       └── file_ops.rs
├── src/
│   ├── index.html
│   ├── style.css
│   └── main.js
└── README.md
```
